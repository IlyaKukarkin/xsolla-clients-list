import * as actions from './actions';
import getters from './getters';
import mutations from './mutations';

const state = {
  flats: {}
};

export default {
  state,
  actions,
  mutations,
  getters
};
